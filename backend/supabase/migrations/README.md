This directory is no longer the canonical migration source.

Use `/Users/sangitaraka/rove-health/supabase/migrations` as the single source of truth for schema changes.

If a backend migration is needed for local tooling, copy it from the root migration set instead of creating new SQL here.
